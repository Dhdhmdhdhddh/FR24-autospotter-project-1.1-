name: Repo Self-Ping

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping GitHub API
        run: |
          curl -I https://api.github.com/repos/${{ github.repository }} > /dev/null
